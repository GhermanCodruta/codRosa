#ifndef _STRATEGYSELECT_h
#define _STRATEGYSELECT_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

uint32_t strategie(byte dips); 

#endif

